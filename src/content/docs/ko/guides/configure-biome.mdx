---
title: Biome 구성 설정
description: Biome 구성이 어떻게 작동하는지 알아보세요.
---

import {FileTree} from '@astrojs/starlight/components';

이 가이드는 Biome을 어떻게 구성 설정하는지 이해하는 데 도움을 줍니다.
이곳에서는 Biome 구성 파일의 구조와 Biome이 구성을 확인하는 방법에 대해 설명합니다.
이미 구성 설정에 익숙하다면 사용 가능한 모든 옵션이 자세히 설명되어 있는 [구성 설정 레퍼런스](/ko/reference/configuration/)를 살펴보는 것이 좋습니다.

Biome에서는 CLI 옵션 또는 `biome.json` 또는 `biome.jsonc`라는 구성 파일을 사용하여 동작을 사용자 지정할 수 있습니다.
각 프로젝트마다 구성 파일을 만드는 것이 좋습니다.
이렇게 하면 각 팀원이 CLI와 Biome 통합을 허용하는 모든 편집기에서 동일한 구성을 갖도록 할 수 있습니다.
구성 파일에서 사용할 수 있는 많은 옵션은 CLI에서도 사용할 수 있습니다.


## 구성 파일 구조

Biome 구성 파일의 이름은 `biome.json` 또는 `biome.jsonc`입니다.
일반적으로 프로젝트의 루트 디렉터리에서 프로젝트의 `package.json` 옆에 위치합니다.

Biome은 툴체인이기 때문에 제공되는 도구를 중심으로 구성이 이루어집니다.
현재 Biome은 포매터, 린터, import 소터(import 오거나이저라고도 함)의 세 가지 도구를 제공합니다.
이 모든 도구는 기본적으로 활성화되어 있습니다.
`<tool>.enabled` 필드를 사용해 이 중 하나 또는 여러 개의 도구를 비활성화할 수 있습니다.

```json title="biome.json"
{
  "$schema": "https://biomejs.dev/schemas/1.9.3/schema.json",
  "formatter": {
    "enabled": false
  },
  "linter": {
    "enabled": false
  },
  "organizeImports": {
    "enabled": false
  }
}
```

두 개 이상의 언어에 적용되는 옵션은 해당 도구 필드에 배치됩니다.
도구의 언어별 옵션은 `<language>.<tool>` 필드 아래에 배치됩니다.
이를 통해 특정 언어에 대한 일반 옵션을 재정의할 수도 있습니다.
언어에 따라 도구를 활성화하거나 비활성화할 수도 있습니다.
다음 예에서는 모든 언어에 대한 일반 옵션인 `formatter.indentStyle`과 `formatter.lineWidth`를 구성합니다.
또한 `javascript.formatter`에서 JavaScript 전용 옵션 `quoteStyle`을 설정하고 `formatter.lineWidth`를 재정의합니다.
JSON 파일에 대해서는 포매터를 비활성화했습니다.

```json5 title="biome.jsonc"
{
  "formatter": {
    "indentStyle": "space", // 기본값: `tab`
    "lineWidth": 100 // 기본값: `80`
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single", // 기본값: `double`
      "lineWidth": 120 // `formatter.lineWidth` 재정의
    }
  },
  "json": {
    "formatter": {
      "enabled": false
    }
  }
}
```

:::note
Biome은 JavaScript 언어의 모든 변형을 `javascript`로 지칭합니다. 여기에는 TypeScript, JSX 및 TSX가 포함됩니다.
:::


## 구성 파일 처리

Biome은 자동 검색을 사용하여 가장 가까운 구성 파일을 찾습니다.
작업 디렉터리와 상위 디렉터리에서 `biome.json` 또는 `biome.jsonc` 파일을 찾을 때까지 찾습니다.
구성을 찾지 못하면 Biome의 기본값을 적용합니다.
`biome.json`과 `biome.jsonc`가 모두 같은 폴더에 있는 경우 `biome.json`을 우선합니다.

다음은 예시입니다.

<FileTree>
- app
  - backend
    - biome.json
    - package.json
  - frontend
    - biome.json
    - legacy
      - package.json
    - new
      - package.json
</FileTree>

- `app/backend/package.json`에서 실행되는 Biome 명령은 `app/backend/biome.json` 구성 파일을 사용합니다.
- `app/frontend/legacy/package.json`과 `app/frontend/new/package.json`에서 실행되는 Biome 명령은 `app/frontend/biome.json` 구성 파일을 사용합니다.

:::caution
Biome은 CLI와 LSP 모두에서 중첩된 `biome.json` 파일을 지원하지 않습니다. [관련 이슈에 문의하여 도움을 받으세요](https://github.com/biomejs/biome/issues/2228)
:::

:::note
Biome 명령은 `--config-path` 옵션과 `BIOME_CONFIG_PATH` 환경 변수 사용을 지원합니다.
이를 통해 사용자 지정 구성 파일 또는 Biome이 `biome.json` 또는 `biome.jsonc` 파일을 찾을 수 있는 디렉터리를 지정할 수 있습니다.
`--config-path` 또는 `BIOME_CONFIG_PATH`를 사용하면 표준 구성 파일 확인 과정이 **비활성화**됩니다.

`--config-path` 또는 `BIOME_CONFIG_PATH`가 파일을 직접 가리키는 경우, `biome.json` 또는 `biome.jsonc`가 아닌 다른 이름을 사용할 수 있습니다.
Biome은 표준 JSON 구문 분석기를 사용하여 .json 파일을 읽습니다.
다른 확장자를 가진 파일의 경우, Biome은 주석과 후행 쉼표를 허용하는 보다 유연한 JSON 구문 분석기를 사용하여 `.jsonc` 파일로 처리합니다.
:::


## 구성 파일 공유

`extends` 필드를 사용하면 여러 파일에 걸쳐 설정을 분할할 수 있습니다.
이렇게 하면 여러 프로젝트 또는 폴더에서 공통 설정을 공유할 수 있습니다.

다음은 `common.json` 구성 파일을 확장하도록 구성을 설정하는 방법의 예시입니다.

```json title="biome.json"
{
  "extends": ["./common.json"]
}
```

`extends`에 정의된 항목은 `biome.json` 파일이 정의된 경로에서 처리됩니다.
나열된 순서대로 처리되며 이후 설정을 이전 설정보다 우선합니다.

Biome은 `node_modules/` 디렉터리에서 구성 파일을 확인할 수 있습니다.
따라서 패키지에서 구성 파일을 내보내고 여러 프로젝트에서 가져올 수 있습니다.

이를 위해 가장 먼저 해야 할 일은 특정 방식으로 ‘공유’ Biome 구성을 설정하는 것입니다.
예를 들어 `@org/shared-configs/biome` 지정자를 사용하는 `@org/shared-configs`라는 패키지의 구성을 공유하고자 한다고 가정해 보겠습니다.
이 패키지의 `package.json`에 `exports` 항목을 만들어야 합니다.

```json title="package.json" ins={5,3}
{
  "name": "@org/shared-configs",
  "type": "module",
  "exports": {
    "./biome": "./biome.json"
  }
}
```

프로젝트에 `@org/shared-configs`가 올바르게 설치되었는지 확인하고 다음 코드처럼 보이도록 `biome.json` 파일을 업데이트합니다.

```json title="biome.json"
{
  "extends": ["@org/shared-configs/biome"]
}
```

Biome은 작업 디렉터리에서 `@org/shared-configs/` 라이브러리를 **처리**하려고 시도합니다.
작업 디렉터리는 다음과 같습니다.

- CLI를 사용하는 경우 스크립트를 실행하는 디렉터리입니다.
  일반적으로 `package.json` 파일의 위치와 일치합니다.
- LSP를 사용하는 경우 프로젝트의 루트 디렉터리입니다.

:::caution
기존 처리 방식이 변경되는 것을 방지하기 위해 점`.`으로 시작하거나 `.json` 또는 `.jsonc`로 끝나는 경로는 `node_modules/`에서 처리되지 **않습니다**.
:::

처리 알고리즘에 대한 자세한 내용은 [Node.js 문서](https://nodejs.org/api/esm.html#resolution-and-loading-algorithm)를 참조하세요.


## 파일 무시

Biome에서 처리할 파일과 디렉터리를 제어하는 첫 번째 방법은 CLI에서 해당 파일과 디렉터리를 나열하는 것입니다.
다음 명령에서는 `file1.js`와 `src` 디렉터리에 있는 모든 파일만 포매팅합니다.
디렉터리는 재귀적으로 탐색됩니다.

```shell
biome format file1.js src/
```

:::caution
명령줄에 사용된 글로브 패턴은 Biome에서 해석되지 않으며 셸에 의해 확장됩니다.
일부 셸은 재귀적 글로브 `**`를 지원하지 않습니다.
:::

Biome 구성 파일을 사용하여 처리할 파일을 구체화할 수 있습니다.
`include`를 사용하여 처리할 파일을, `ignore`를 사용하여 처리하지 않을 파일을 명시적으로 나열할 수 있습니다.
`include`와 `ignore`는 `src/**/*.js`와 같은 글로브 패턴을 허용합니다.
지원되는 글로브 구문은 [관련 섹션](#글로브-문법-설명)을 참조하세요.
`include`는 항상 `ignore`보다 먼저 적용됩니다.
이를 통해 일부 파일을 포함시키고 포함시킨 파일 중 일부를 무시할 수 있습니다.

:::note
`include`와 `ignore`는 약간 방식이 다릅니다.
`include`는 Biome이 폴더를 탐색하는 것을 방지하지 않습니다.
즉, Biome이 폴더를 탐색하지 못하도록 하려면 폴더를 `ignore`에 추가해야 합니다.
:::

Biome은 모든 도구에 적용되는 전역 `files.include` 및 `files.ignore` 필드를 제공합니다.
또한 `<tool>.include` 및 `<tool>.ignore`를 사용하여 도구 수준에서 파일을 포함하거나 무시할 수 있습니다.
이 필드들은 전역 `files.include` 및 `files.ignore`를 재정의하지 않습니다.
`files.include` 및 `files.ignore`는 도구의 `include` 및 `ignore`보다 먼저 적용됩니다.

다음 구성을 예로 들어 보겠습니다.

```json title="biome.json"
{
  "files": {
    "include": ["src/**/*.js", "test/**/*.js"],
    "ignore": ["**/*.min.js"],
  },
  "linter": {
    "ignore": ["test"]
  }
}
```

그리고 다음 명령을 실행합니다.

```shell
biome format test/
```

이 명령은 `test` 디렉터리에서 확장자가 `.js`로 끝나고 `.min.js`로 끝나지 않는 파일을 포매팅합니다.
`src` 내 파일은 CLI에 나열하지 않았기 때문에 포매팅되지 않습니다.

다음 명령을 실행하면 `test` 디렉터리가 린터에 대해 명시적으로 무시되므로 어떤 파일도 린팅되지 않습니다.

```shell
biome lint test/
```

Biome은 작업 디렉터리에서 상대적으로 글로브를 확인합니다.
작업 디렉터리는 일반적으로 CLI 명령을 실행하는 디렉터리입니다.
따라서 구성 파일이 명령을 실행하는 위치와 다른 디렉터리에 있는 경우 **특히 주의**해야 합니다.
에디터(LSP)의 경우 작업 디렉터리는 프로젝트의 루트 디렉터리입니다.

두 디렉터리 `backend/`와 `frontend/`, 그리고 앞서 소개한 Biome 구성 파일이 포함된 프로젝트를 예로 들어 보겠습니다.
`frontend/` 디렉터리 안에 `package.json`은 Biome 포매터를 실행하는 `format` 스크립트를 지정합니다.

<FileTree>
- backend
  - ...
- biome.json
- frontend
  - package.json
  - src
    - ...
  - test
    - ...
</FileTree>

```json title="frontend/package.json"
{
  "name": "frontend-project",
  "scripts": {
    "format": "biome format --write ./"
  }
}
```

`frontend/package.json`에서 스크립트 `format`을 실행하면 해당 스크립트에서 처리되는 작업 디렉터리는 `frontend/`가 됩니다.
글로브 `src/**/*.js`와 `test/**/*.js`의 ‘기본’ 디렉터리는 `frontend/`가 됩니다.
따라서 `frontend/src/`와 `frontend/test/`의 파일만 포매팅됩니다.

```json title="biome.json"
{
  "files": {
    "include": ["src/**/*.js", "src/**/*.ts"],
    "ignore": ["test"]
  },
  "formatter": {
    "indentStyle": "space"
  }
}
```

:::caution
`overrides` 내부의 `ignore`와 `include`는 약간 방식이 다릅니다.
- `ignore`: 파일이 글로브와 일치하면 이 재정의 내부의 구성을 적용하지 않고 다음 재정의를 계속 적용합니다.
- `include`: 파일이 글로브와 일치하면 이 재정의 내부의 구성을 적용하고 다음 재정의를 계속 적용합니다.
:::

:::note
기본적으로 Biome은 **보호된 파일**로 다뤄지는 일부 파일은 항상 무시합니다.
즉, 이러한 파일에 대해서는 Biome에서 진단하지 않습니다.
현재 다음 파일이 보호되고 있습니다.

- `composer.lock`
- `npm-shrinkwrap.json`
- `package-lock.json`
- `yarn.lock`
:::

:::note
[VCS에서 무시한 파일을 무시](/ko/guides/integrate-in-vcs#파일-무시-사용)할 수도 있습니다.
:::


## 잘 알려진 파일

다음은 확장자가 아니라 파일 이름에 따라 특별히 처리되는 잘 알려진 몇 가지 파일입니다.
현재 잘 알려진 파일은 JSON과 닮은 파일만 있지만, 새로운 구문 분석기를 지원하게 되면 다른 유형까지 포함하도록 목록을 확장할 수 있습니다.

다음 파일은 `json.parser.allowComments` 및 `json.parser.allowTrailingCommas` 옵션이 모두 `false`로 설정된 `JSON` 파일로 구문 분석됩니다.

- `.all-contributorsrc`
- `.arcconfig`
- `.auto-changelog`
- `.bowerrc`
- `.c8rc`
- `.htmlhintrc`
- `.imgbotconfig`
- `.jslintrc`
- `.nycrc`
- `.tern-config`
- `.tern-project`
- `.vuerc`
- `.watchmanconfig`
- `mcmod.info`

다음 파일은 `json.parser.allowComments` 옵션이 `true`로 설정된 `JSON` 파일로 구문 분석되지만 `json.parser.allowTrailingCommas` 옵션은 `false`로 설정되어 있습니다.
이는 이러한 파일을 사용하는 도구가 주석만 제거할 수 있기 때문입니다.

- `.ember-cli`
- `.eslintrc.json`
- `.jscsrc`
- `.jshintrc`
- `tslint.json`
- `turbo.json`

다음 파일은 `json.parser.allowComments` 및 `json.parser.allowTrailingCommas` 옵션이 `true`로 설정된 `JSON` 파일로 구문 분석됩니다.
이는 이러한 파일을 사용하는 도구가 이러한 설정을 수용하도록 설계되었기 때문입니다.

- `.babelrc`
- `.babelrc.json`
- `.devcontainer.json`
- `.hintrc`
- `.hintrc.json`
- `.swcrc`
- `api-documenter.json`
- `api-extractor.json`
- `babel.config.json`
- `deno.json`
- `devcontainer.json`
- `dprint.json`
- `jsconfig.json`
- `jsr.json`
- `language-configuration.json`
- `tsconfig.json`
- `typedoc.json`
- `typescript.json`


## 글로브 문법 설명

글로브 패턴은 파일 이름 집합을 지정합니다.
Biome은 다음과 같은 글로브를 지원합니다.

- `*`는 0개 이상의 문자와 일치합니다. 경로 구분 기호 `/`와는 일치하지 않습니다.
- `**`는 디렉터리, 파일과 재귀적으로 일치합니다.
  이 시퀀스는 **반드시** 단일 경로 구성 요소를 형성해야 하므로 `**a`와 `b**`는 모두 유효하지 않으며 오류가 발생합니다.
  두 개 이상의 연속 `*` 문자로 이루어진 시퀀스도 유효하지 않습니다.
- `[...]`는 대괄호 안의 모든 문자와 일치합니다.
  Unicode 순서에 따라 문자 범위도 지정할 수 있으며 예를 들어 `[0-9]`는 0에서 9 사이의 모든 문자를 지정합니다.
- `[!...]`는 `[...]`의 부정이며 괄호 안에 **없는** 모든 문자와 일치합니다.

다음은 몇 가지 예시입니다.

- `dist/**`는 `dist` 디렉터리와 이 디렉터리에 있는 모든 파일과 일치합니다.
- `**/test/**`는 파일 위치에 관계없이 `test`라는 이름의 디렉터리에 있는 모든 파일과 일치합니다. 예: `dist/test`, `src/test`.
- `**/*.js`는 모든 디렉터리에 있는 확장자가 `.js`로 끝나는 모든 파일과 일치합니다.

Biome은 모든 글로브에 `**/` 접두사가 있는 것으로 취급하는 글로브 라이브러리를 사용합니다.
즉, `src/**/*.js`와 `**/src/**/*.js`는 동일한 것으로 취급되며 두 패턴 모두 `src/file.js`, `test/src/file.js`와 일치합니다.

:::caution
이러한 패턴은 Biome 구성 파일에서 사용할 수 있습니다.
명령줄에 사용된 글로브 패턴은 Biome에서 해석되지 않으며 셸에 의해 확장됩니다.
일부 셸은 재귀적 글로브 `**`를 지원하지 않습니다.
:::
